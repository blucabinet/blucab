services:
  app:
    build: blucab
    container_name: django
    ports:
      - '8000:8000'
    volumes:
      - ./app_db:/app/db
    env_file:
      - ./.env.dev
    #environment:
    #  - DEBUG=False
